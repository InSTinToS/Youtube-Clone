import OriginalButton from 'frontend/components/Form/Button'

import styled from 'styled-components'

export const Button = styled(OriginalButton)`
  position: fixed;
  right: 16px;
  bottom: 16px;
`

export const Container = styled.main`
  display: flex;
  align-items: center;
  justify-content: center;
`
